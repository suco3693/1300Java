package recitation.pkg10;

import java.io.File;
import java.io.FileNotFoundException;
import java.util.Scanner;

public class StringSimilarity {
    //Reads from file and returns the DNA sequence in the file
    public String readFile(String fileName) throws FileNotFoundException {
       /***write the code to read the file with name fileName using Scanner***/
       String DNA = new String();
        //Create a File object
       //Create a Scanner object
       //Use next() function on Scanner object you just created to read the whole string in one step
       return DNA;
    }
    /***Write a function that gets two strings as parameters and calculates hamming distance between them***/
    
    /***Write a function that calculates the similarity score using hamming distance function***/
  

    public static void main(String[] args) throws FileNotFoundException {
        StringSimilarity sim = new StringSimilarity();
      
        //Calculate human and unknwon similarity
        //Calculate mouse and unknwon similarity
        
       //Compare the two similarity scores and decides if the unknown is more similar to a human or a mouse
    }
}
